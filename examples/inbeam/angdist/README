================================================================================
Example fit with gamma-ray angular distributions

             This example is identical to ./examples/inbeam/fit
	     (see ./examples/inbeam/fit for a detailed description)
	     except that it uses UCGretina_AD, compiled with the AD
	     flag set, enabling optional non-isotropic gamma-ray
	     angular distributions. The coeeficients a0, a2, a4
	     for each transition are the last three entries in the
	     transition lines in the level data file
	     z16.a44.ad.lvldata.

	     The script angDist.C plots the emitted angular 
	     distributions of the 1329 keV (a0,a2,a4 = 0, 0.3, 0.02,
	     red) and 2150 keV (isotropic, blue) transitions. 

================================================================================
